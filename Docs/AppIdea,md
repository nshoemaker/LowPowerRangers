The app idea is to make an Android mobile application which can use data it receives from the server to show the most recent position of the tag being tagged. It will allow the user to add new tags by entering their tag ID and a name for them. 
